import 'package:flutter/material.dart';

class ContactworkerPage extends StatelessWidget {
  const ContactworkerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Contact worker')),
      body: const Center(child: Text('Contact worker Page')),
    );
  }
}
