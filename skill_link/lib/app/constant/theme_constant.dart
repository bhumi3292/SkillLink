import 'package:flutter/material.dart';

class ThemeConstant {
  ThemeConstant._();
  static const Color darkPrimaryColor = Color(0xFF003366);
  static const Color primaryColor = Color(0xFF003366);
  static const Color appBarColor = Color(0xFF003366);
}

// AppColors class for backward compatibility
class AppColors {
  AppColors._();
  static const Color primaryColor = Color(0xFF003366);
  static const Color darkPrimaryColor = Color(0xFF003366);
  static const Color appBarColor = Color(0xFF003366);
}